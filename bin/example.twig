<!DOCTYPE html>
<html>
<head>
</head>
<body>

{% for c in albumstwig %}
<div>
    <ul>
        <li>Album Name: {{c.name}}</li>
        <li>Artist: {{c.artist}}</li>
        <li>Genre: {{c.genre}}</li>
        <li>Year: {{c.year}}</li>
    </ul>
</div>
{% endfor %}


<script>
/*
var xhr = new XMLHttpRequest();
xhr.open('GET', '/albumjson');
xhr.onload = function(evt) {
   if (xhr.status === 200) {
        var response = JSON.parse(xhr.responseText);

        console.log(response);
    } else {
        alert('Request failed.  Returned status of ' + xhr.status);
    }
};
xhr.send();
*/
</script>

</body>
</html>
